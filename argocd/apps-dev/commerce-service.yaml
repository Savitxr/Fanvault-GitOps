apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-commerce-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Fanvault-CloudOps/Fanvault-GitOps.git
    targetRevision: HEAD
    path: charts/commerce-service
    helm:
      valueFiles:
        - values.yaml
        - ../../environments/dev/values-commerce.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
